# Streamlit + auth Entra (OIDC).
# Authlib est ÉPINGLÉ à 1.3.2 : les versions >= 1.6 cassent st.login
# (set_state_data écrit dans session=None -> "'NoneType' object does not
#  support item assignment"). Ne pas mettre à jour sans revérifier le login.
streamlit==1.50.0
Authlib==1.3.2
msal==1.37.0
requests==2.32.5
